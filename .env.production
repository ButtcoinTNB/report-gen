# Production Environment Variables
NODE_ENV=production

# Disable static optimization for pages that use getServerSideProps
NEXT_DISABLE_SSGOPT=true

# Ensure Next.js knows we're in a server-side environment
NEXT_RUNTIME=nodejs 